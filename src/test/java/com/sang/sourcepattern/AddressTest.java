package com.sang.sourcepattern; import org.springframework.beans.factory.annotation.Autowired; import org.springframework.boot.test.context.SpringBootTest; import org.junit.jupiter.api.Test; import com.sang.sourcepattern.service.BookingService; import com.sang.sourcepattern.entity.Booking; import com.sang.sourcepattern.repository.BookingRepository; @SpringBootTest public class AddressTest { @Autowired BookingService bookingService; @Autowired BookingRepository bookingRepository; @Test public void test() { System.out.println("ADDRESS: " + bookingRepository.findByIdWithServices(1).get().getUser().getAddress()); System.out.println("DTO: " + bookingService.getBookingById(1, "shop1@gmail.com").getCustomerAddress()); } }
